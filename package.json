{
  "name": "alessanna-platform",
  "version": "1.0.0",
  "private": true,
  "description": "Alessanna salon: public static site + CRM (work/)",
  "scripts": {
    "work:dev": "npm run dev --prefix work",
    "work:build": "npm run build --prefix work",
    "vercel:static": "node scripts/vercel-static-bundle.mjs"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  }
}
